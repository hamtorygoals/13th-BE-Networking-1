# Database credentials
DB_ROOT_PASSWORD=your_root_password_here
DB_USERNAME=cotato_user
DB_PASSWORD=your_db_password_here
DB_NAME=cotato_networking

# Docker image to use (replace with your Docker Hub username)
# Local build:  DOCKER_IMAGE=cotato-backend:latest
# Docker Hub:   DOCKER_IMAGE=your-dockerhub-username/cotato-backend:latest
DOCKER_IMAGE=cotato-backend:latest
